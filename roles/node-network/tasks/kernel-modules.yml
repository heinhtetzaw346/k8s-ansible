- name: Configure kernel modules
  become: true
  tags: [ node-network, kernel ]
  block:

    - name: Load Kernel Modules persistently
      community.general.modprobe:
        name: "{{ item }}"
        state: present
        persistent: present
      loop: "{{ base_kernel_modules }}"

    - name: Load IPVS Kernel Modules persistently
      when: ipvs_enabled | bool
      community.general.modprobe:
        name: "{{ item }}"
        state: present
        persistent: present
      loop: "{{ ipvs_kernel_modules }}"

    - name: Load Additional Kernel Modules persistently
      when: additional_kernel_modules is defined
      community.general.modprobe:
        name: "{{ item }}"
        state: present
        persistent: present
      loop: "{{ additional_kernel_modules }}"
