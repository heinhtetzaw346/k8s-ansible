- name: Set timezone
  become: true
  tags: [ node-preflight, timezone ]
  block:
    - name: Ensure tzdata
      ansible.builtin.package:
        name: tzdata
        state: present

    - name: Set timezone
      community.general.timezone:
        name: "{{ timezone }}"
