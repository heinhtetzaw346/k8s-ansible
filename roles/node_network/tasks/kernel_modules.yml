- name: Configure kernel modules
  become: true
  block:
    - name: Load Kernel Modules persistently
      community.general.modprobe:
        name: "{{ item }}"
        state: present
        persistent: present
      loop: "{{ kernel_modules }}"
