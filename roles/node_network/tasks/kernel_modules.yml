- name: Configure kernel modules
  become: true
  tags: [ node_network, kernel ]
  block:
    - name: Load Kernel Modules persistently
      community.general.modprobe:
        name: "{{ item }}"
        state: present
        persistent: present
      loop: "{{ kernel_modules }}"
