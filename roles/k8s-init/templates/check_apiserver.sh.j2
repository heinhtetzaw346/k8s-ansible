#!/bin/sh
# Managed by k8s-ansible

errorExit() {
    echo "*** $*" 1>&2
    exit 1
}

curl -sfk --max-time 2 https://localhost:{{ control_plane_lb_port }}/healthz -o /dev/null || errorExit "Error GET https://localhost:{{ control_plane_lb_port }}/healthz"
